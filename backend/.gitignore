# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
env/
.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# Streamlit secrets (NEVER commit this!)
.streamlit/secrets.toml
.streamlit/*.txt

# FastAPI / AutiStudy secrets (OpenAI key, Gmail App Password — NEVER commit)
config/secrets.toml
.env
.env.*

# Data files (sessions, chats, OTP hashes — temporary / sensitive)
data/sessions.json
data/chats.json
data/email_otps.json
data/invite_rate_limits.json
data/parent_sessions.json

# Keep users.json - contains registered accounts

# ChromaDB (too large for GitHub, will need separate hosting)
shared_chroma_db/
chroma_db/
*.db

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# API keys
*api_key*.txt
*_key.txt
