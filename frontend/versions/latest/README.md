# AutiStudy Frontend — `latest` snapshot

Archived snapshot of the `frontend/` folder at tag **`v4.1-adaptive-agent`** (June 2026).

The **live codebase** is the parent `frontend/` directory inside `AutiStudy-App/`.
For current architecture documentation see `frontend/docs/AUTISTUDY WORKING - latest.md`.
